from machine import Pin, I2C
from i2c_lcd import I2cLcd
from time import sleep
i2c = I2C(0, scl=Pin(22), sda=Pin(21), freq=400000)
Dirección común del LCD I2C: 0x27 o 0x3F
lcd = I2cLcd(i2c, 0x27, 2, 16)
lcd.clear()
lcd.move_to(0, 0)
lcd.putstr("ANDRES P")
lcd.move_to(0, 1)
lcd.putstr("MATIAS C")
